import { readFile } from 'fs/promises';
import { resolve } from 'path';
import { __dirname } from './const.ts';
import { db } from './database.ts';

await db.query(await readFile(resolve(__dirname, '{{filename}}.surql'), 'utf-8'));
